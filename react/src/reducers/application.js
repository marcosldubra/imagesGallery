//import { combineReducers } from 'redux';
//import images, * as fromImages from 'reducers';


//export default combineReducers({
  //images,
//})

//export const getImages = state => fromImages.getImages(state.images);
